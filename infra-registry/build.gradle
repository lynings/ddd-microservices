mainClassName = 'pers.lynings.springcloud.registry.RegistryApplication'

bootJar {
    baseName = 'infra-registry'
    version = '0.1'
}

dependencies {
    compile('org.springframework.cloud:spring-cloud-starter-netflix-eureka-server')
}
