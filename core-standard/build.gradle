apply plugin: 'org.springframework.boot'

jar {
    baseName = 'core-standard'
    version = '0.0.1-SNAPSHOT'
}

dependencies {
    compile 'org.springframework.boot:spring-boot-starter-webflux',
            'org.springframework.boot:spring-boot-starter-data-jpa'
}

